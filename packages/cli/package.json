{
  "name": "grndctrl-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Dev-only text board. Makes the correlation engine demonstrable before any UI exists.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "grndctrl-cli": "./bin/grndctrl-cli.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@grndctrl/core": "*",
    "@napi-rs/keyring": "^1.3.0"
  }
}
