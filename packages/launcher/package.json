{
  "name": "grndctrl",
  "version": "0.5.0",
  "description": "Ground Control: a local-first console for your Jira tickets and the AI agents working them. Run it with `npx grndctrl`. This package resolves the Electron runtime, verifies it, and spawns the app.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "grndctrl": "./bin/grndctrl.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@grndctrl/desktop": "0.5.0"
  },
  "files": [
    "bin",
    "dist",
    "!dist/.tsbuildinfo",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jonupchurch/grndctrl.git",
    "directory": "packages/launcher"
  },
  "homepage": "https://github.com/jonupchurch/grndctrl#readme"
}
