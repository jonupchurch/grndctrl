{
  "name": "grndctrl",
  "version": "0.0.0",
  "private": true,
  "description": "Ground Control launcher \u2014 resolves the Electron runtime, verifies it, and spawns the app.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "grndctrl": "./bin/grndctrl.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@grndctrl/desktop": "0.0.0"
  },
  "files": [
    "bin",
    "dist",
    "!dist/.tsbuildinfo",
    "README.md"
  ]
}
